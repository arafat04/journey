### Probability Theory:

1. what is sample space $\Omega$
Ans: Sample sample is a set that contains all the possible outcomes for a random experiment. If we toss a coin we can not predict the outcome as this a random experiment. However, if we take all the possible outcomes, that makes the sample sample space.

2. What is an event?

   Ans: An **event** $A$ is a subset of the sample space that answers a particular question. For instance, for a fair coin toss, the sample space is

$$
S = \{H, T\}.
$$

If we define the event

$$
A = \{H\},
$$

then the event occurs when the outcome is a head. In this case, the event contains a single outcome.

Since every subset of the sample space is an event, a sample space with $n$ outcomes has $2^n$ possible events.
   
3. What is probability?

   Ans: Probability is the measurement how likely an event is to happen and expressed in between 0 and 1.
   Run a random experiment many times and count the ocurences that satisfies the question of an event. For instance, $A = \{\text{toss a head}\}$. If we toss the coin 100 times, count how many times we get a head. When done this in series, for each series, divide the number of times the outcome is favorable for event A by the total number of trials in that series. This is called the probability of the event A $p(A). For each series, the probability will remain close to a constant value. 
6. When we can measure probability using this formula:

$$Probability = \frac{\text{number of outcomes}}{\text{total number of possible outcomes}}$$

5. What are the types of methods for calculating probability?
6. Why we nee Maximum Likelihood Estimation (MLE)?

# Speech and Dialogue systems

1. Basic components of a dialogue system.

  Ans: Basic components of a dialogue system includes:

      1. 
2. Natural language understanding in dialogue systems.
3. Dialogue state tracking.
4. Methods for dialogue management [MDP and POMDP systems; reinforcement learning; user simulation].
5. End-to-end neural dialogue systems.
6. Open-domain dialogue system architectures.
7. Natural language generation.
8. Dialogue systems evaluation.
9. Visual dialogue and multimodal systems.
